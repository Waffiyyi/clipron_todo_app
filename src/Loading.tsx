export const Loading = () => {
    return (
        <main className="flex justify-center items-center min-h-screen bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">
            <div className="text-center">
                <h1>Loading ...</h1>
                <p>Hopefully not for too long :)</p>
            </div>
        </main>
    );
};
